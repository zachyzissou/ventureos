{
  "name": "ventureos",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "description": "VentureOS core libraries (role cards, enforcement, handoff validation)",
  "scripts": {
    "test": "jest --runInBand",
    "build": "tsc -p tsconfig.json --noEmit",
    "role-cards:render": "node -r ts-node/register ./scripts/render-role-card-prompts.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "better-sqlite3": "^12.6.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.11.30",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
