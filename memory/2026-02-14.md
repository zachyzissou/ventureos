# 2026-02-14

## GitLab Connectivity Fix
- Root cause: `~/.mcporter/mcporter.json` had stale IP `192.168.225.149:9080` (old GitLab host). Fixed to `slurpnet:9080` which resolves via `/etc/hosts` → `192.168.225.196`.
- `~/clawd/config/mcporter.json` (template) also updated.
- Monitor script (`scripts/monitor-openclaw.sh`) was firing P1: auth_or_timeout_errors from accumulated gateway.err.log entries. Reset cursor to clear.
- `glab` CLI config at `~/Library/Application Support/glab-cli/config.yml` confirmed working with `slurpnet:9080`.
- User added `slurpnet.local` to network DNS; `/etc/hosts` still needs the `.local` alias added (requires sudo).
- Note: `slurpnet.local` does NOT resolve via curl on Mac Studio (mDNS latency). Bare `slurpnet` is the reliable hostname.
- `dscacheutil` resolves `slurpnet.local` → `192.168.225.196` (mDNS works at OS level), but curl times out (exit 28). `/etc/hosts` alias not yet added (needs sudo).

## Mistake: slurpnet → slurpnet.local rename (REVERTED)
- User said "gitlab can be reached at slurpnet.local:9080" — I bulk-renamed `slurpnet:9080` → `slurpnet.local:9080` across all configs (glab, mcporter, Echo SOUL/MEMORY/docs, main MEMORY.md).
- **This broke everything** because `slurpnet.local` doesn't resolve on Mac Studio (no mDNS response, no /etc/hosts entry).
- Reverted all changes back to `slurpnet:9080`. Lesson: verify DNS resolution BEFORE bulk-renaming hostnames.

## mcporter Config: Live vs Template
- `~/.mcporter/mcporter.json` is the LIVE config mcporter reads at runtime.
- `~/clawd/config/mcporter.json` is a template/copy — editing it does NOT affect running agents.
- The stale `192.168.225.149` IP was in the live config, not the template. Always fix `~/.mcporter/mcporter.json`.

## memory-observation-sync Cron Fix
- Archivist's `memory-observation-sync` job (id: `de6963c8-5f74-4527-a28a-4c86bafb6feb`) was failing because it ran `cat` on today's daily memory file before checking if it exists.
- On days with no activity logged yet, the file doesn't exist → `cat` fails → codex-mini reports it as FAILED instead of following the "if no file, HEARTBEAT_OK" instruction.
- Fix: updated prompt to use `[ ! -f "$FILE" ] || [ ! -s "$FILE" ]` check before cat, outputting `NO_FILE_OR_EMPTY` sentinel that the agent treats as clean HEARTBEAT_OK.
- Daily memory files (`memory/YYYY-MM-DD.md`) are created organically during work — there's no dedicated "create today's file" job. Missing file early in the day is normal.

## Echo Workspace Files Updated
All Echo workspace files confirmed clean of stale GitLab refs:
- `~/.openclaw/agents/echo/agent/SOUL.md` — GitLab base URL
- `~/.openclaw/workspace-echo/MEMORY.md` — all refs
- `~/.openclaw/workspace-echo/ROADMAP.md` — all issue links
- `~/.openclaw/workspace-echo/README_UPDATED.md` — all project links
- `~/.openclaw/workspace-echo/docs/AGENT-ARCHITECTURE.md`
- `~/.openclaw/workspace-echo/docs/OPTIMIZATION-GUIDE.md`
- Build cache repos (`~/.openclaw/workspace-echo/builds/`) have stale remotes but will self-heal on next CI clone.
- Cron scripts verified clean. No stale refs in any cron job prompts.

## Gateway Error Log
- `~/.openclaw/logs/gateway.err.log` had 35K+ accumulated ECONNREFUSED errors from old IP `192.168.225.149:9080`.
- Monitor script cursor advanced past these to prevent re-firing P1 alerts.
- Monitor now reports `HEARTBEAT_OK`.

## Next Steps (Post-Fix)
- Monitor next Echo cron run to confirm ECONNREFUSED alerts fully stopped.
- If other agents (Atlas, Synth, Oracle, Sentinel) hit old URLs, update their SOUL.md/MEMORY.md.
- Check git remote URLs on local repo clones if connectivity issues persist.

## GitLab Hostname Reference
- Canonical hostname: `slurpnet:9080` (resolves via `/etc/hosts` → `192.168.225.196`)
- Old stale IP: `192.168.225.149` (no longer valid, was Plane.so box)
- `slurpnet.local:9080` — user wants this to work; added to network DNS but Mac Studio can't resolve it yet via curl
- Token `glpat-fKIVkcQdU933x0SRzpu92286MQp1OjEH.01.0w0vh6acn` (glab) confirmed valid, returns 200 on /api/v4/user
