# Daily Memory Log - 2026-02-11

## Multi-Agent Channel Audit & Fixes
- Reviewed all 6 VentureOS role channels (oracle, atlas, sentinel, verifier, archivist, synth) — excluding echo-mission-control per user request.
- Found 3 systemic issues: (1) All dispatch messages from Mission Control used raw `curl` + hardcoded GitLab API token instead of `mcporter gitlab`, (2) GitLab token exposed in plaintext in session transcripts, (3) Sentinel crashed (context overflow 207k > 200k), Verifier stuck in confused "announce" loop.
- **Fix**: Updated Echo (Mission Control) SOUL.md at `/Users/zachgonser/.openclaw/agents/echo/agent/SOUL.md` — added `## GitLab (CRITICAL)` section forbidding curl/hardcoded tokens, directing agents to use `mcporter` with the `gitlab` server instead.
- Reset Sentinel and Verifier sessions via sessions_send /reset — both confirmed operational after reset.
- Verified `mcporter` with `gitlab` server is functional (108 tools available).

## Home Assistant URL Change
- HA IP changed from `192.168.225.133` to `192.168.225.132` (also accessible via `homeassistant.local:8123`).
- Updated credentials at `/Users/zachgonser/.openclaw/credentials/homeassistant.json` — verified working.
- SSH host key updated for new IP (StrictHostKeyChecking=no on first connect).

## Home Assistant - Middle Bed TV Automation (HomeKit/Siri)
- Problem: HomeKit exposes Control4 media zones (media_player.middle_bed) as TV accessories, but selecting a source in HomeKit only tells the Control4 matrix to switch — doesn't power on the Samsung TV or switch HDMI input.
- Created automation `middle_bed_source_roku` in `/config/automations.yaml` — triggers when `media_player.middle_bed` source attribute changes to "Roku 2".
- Automation flow: WOL magic packet → power on Samsung TV → if TV was off wait 10s, if already on wait 2s → send `KEY_HDMI1` (direct port, not cycling).
- Initially used double `KEY_HDMI` (cycling) which overshot to antenna — switched to direct `KEY_HDMI1` command. Roku is on HDMI 1 of the Middle Bed Samsung TV.
- Updated `scripts.yaml` to also use `KEY_HDMI1` instead of double `KEY_HDMI` cycling.
- Siri command "Hey Siri, watch Roku in the middle bed" confirmed working via HomeKit → source change → automation trigger.
- automations.yaml was previously empty (`[]`).

## Agent Rename: Echo → Nexus (Mission Control)
- Renamed the Mission Control agent from "Echo" to "Nexus" to resolve naming collision (both `main` and `echo` agents posted as the same Discord bot user "Echo").
- Updated: SOUL.md (`/Users/zachgonser/.openclaw/agents/echo/agent/SOUL.md`), gateway config (`name: "Nexus"`, `identity.name: "Nexus"`, `identity.theme: "Mission Control — orchestrator for the VentureOS agent squad"`).
- Renamed Discord channel `#echo-mission-control` → `#nexus-mission-control` (channel ID `1470210601879076914`); updated topic.
- Updated channel system prompt in gateway config — replaced all "Echo" refs with "Nexus", updated dispatch rules to use `sessions_spawn`.
- Agent ID in gateway remains `echo` (only display name changed).

## Multi-Agent Dispatch Routing Fix
- Root cause: `agents_list` returned empty because `subagents.allowAgents` was not configured.
- Fix: Added `subagents.allowAgents` array with all 8 agent IDs (`main`, `echo`, `oracle`, `atlas`, `sentinel`, `verifier`, `archivist`, `synth`) to both `main` and `echo` agent configs in gateway.
- Gateway restarted (SIGUSR1 to PID 86548).
- **Not yet verified end-to-end** — config is in place but no confirmed test of Nexus dispatching to team agents via `sessions_spawn`.

## HomeKit Camera Integration — Architectural Dead End
- Added `camera` domain to HASS Bridge; configured 9 Speco Blue cameras with `video_codec: copy`.
- **Result: Bridge mode cannot stream camera feeds** — HomeKit architectural limitation. Cameras in bridge mode appear as entities but cannot provide video streams.
- Fix requires **accessory mode**: 9 individual HomeKit accessories (ports 21073-21081), one per camera. Each must be paired separately in the Home app.
- Awaiting user approval before implementing.
- Deleted accidental standalone camera HomeKit instance (entry_id `01KH7X9KAP259NFCH2068N9MEJ`, port 21073).
- Learned HA options flow API: `/api/config/config_entries/options/flow` — multi-step forms (init → exclude/include → cameras). Used for all bridge config changes.
- **Critical lesson**: Never edit HA config files via SSH pipe-through-jq — truncated `config_entries` from 42→6 entries. Recovered from `/tmp/config_entries_cam.json`. Use HA REST API exclusively.
- Details in `memory/2026-02-11-homekit-cameras.md`.

## Discord Channel/Agent ID Reference
- `#echo` = `1468796657452711947` → main agent (user-facing "Echo")
- `#nexus-mission-control` = `1470210601879076914` → echo agent (orchestrator "Nexus")
- `#oracle-research` = `1470210648624599192`, `#atlas-infra` = `1470210649786159348`
- `#sentinel-governance` = `1470210650855837696`, `#verifier-qa` = `1470210652143354077`
- `#archivist-knowledge` = `1470210653154185298`, `#synth-factory` = `1470210654819451024`
- Guild: `825047055688532049`
